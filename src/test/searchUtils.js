const searchData = (d) => {
  console.log('search', d)
  // 데이터를 검색하고 결과를 반환하는 로직을 수행합니다.
  // 실제 검색 로직은 이 곳에서 구현합니다.
  // 예: 데이터베이스 또는 외부 API와 상호 작용하여 검색 결과 반환
  const searchResult = d;
  return searchResult;
};

export default searchData;